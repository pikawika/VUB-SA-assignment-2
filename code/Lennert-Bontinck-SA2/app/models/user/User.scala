package models.user

/**
 * Simple case class to represent a user object that has a username and password.
 */
case class User(
                 username: String,
                 password: String
               )
